# 진행상황 (Progress Log)

전체 계획: [`plan.md`](plan.md) · 단계별 작업목록: [`docs/phases/`](docs/phases/)

이 파일은 작업이 진행될 때마다 갱신한다. 각 단계 항목의 상태는 `⬜ 시작 전 / 🟨 진행 중 / ✅ 완료 / ⛔ 보류`로 표기하고, 완료 시 날짜와 핵심 결과를 한 줄로 남긴다.

## 단계별 상태

| 단계 | 상태 | 시작일 | 완료일 | 비고 |
|---|---|---|---|---|
| [Phase 1 — 텍스트 채팅 MVP](docs/phases/01-text-chat-mvp.md) | ✅ 완료 | 2026-07-18 | 2026-07-18 | 감정 라벨 붙는 WS 텍스트 챗봇 + 타이핑 중 로컬 감정 미리보기, E2E 검증 완료 |
| [Phase 2 — 정적 아바타 렌더링](docs/phases/02-static-avatar-rendering.md) | ✅ 완료 | 2026-07-18 | 2026-07-18 | 22종 변환, Konva 렌더링, 캐릭터 선택 UI까지 E2E 검증 완료 |
| [Phase 3 — 완전 자동 레이아웃](docs/phases/03-full-auto-layout.md) | ✅ 완료 | 2026-07-18 | 2026-07-19 | 4단계 전부 완료. talkTo 기반 캐릭터 회전만 프로토콜 미비로 Phase 4로 명시적 이연 |
| [Phase 4 — 전체 프로토콜 + 멀티룸](docs/phases/04-full-protocol-multiroom.md) | ⬜ 시작 전 | | | |
| [Phase 5 — AI 캐릭터](docs/phases/05-ai-character.md) | ⬜ 시작 전 | | | |
| [Phase 6 — Tauri 빌드 + 마무리](docs/phases/06-tauri-packaging.md) | ⬜ 시작 전 | | | |

## 활동 로그 (최신순)

<!-- 새 항목은 이 줄 바로 아래에 추가 (최신이 위로) -->

- **2026-07-19** — 사용자 요청으로 그리디 배치(`placement.ts`)만 따로 떼어 `panel.cpp`(`ComputeDisplacementPenalty`/`AddTalkTos`/`EvalPair`/`EvalPlacement`/`DoGreedyOrdering`/`UpdateHistoresis`) 전체를 다시 한 줄씩 재대조. `EvalPair`에서 실제 불일치 발견: 원작은 `talkTo` 배열에서 상대와 일치하는 항목마다 페널티를 반복 적용하는데, 포팅 코드는 `.includes()`로 존재 여부만 한 번 확인해 매칭 시 한 번만 적용하고 있었다 — talkTo에 중복 대상이 있을 때만 차이가 나고, 지금은 프로토콜에 talkTo가 없어 항상 빈 배열이라 현재는 도달 불가능하지만 Phase 4에서 문제될 수 있어 원작과 동일하게 반복 합산하도록 수정, 회귀 테스트 추가. 나머지 함수는 전부 정확히 일치 재확인. 워크스페이스 전체 typecheck/test(174개)/lint 클린.
- **2026-07-19** — Phase 3 4단계(시드 기반 재현성) 완료로 Phase 3 전체 완료. 착수 전 체크리스트를 실제 구현에 맞춰 재검토: 그리디 배치/줌은 애초에 RNG를 안 쓰는 순수 결정 함수(Stage 1에서 이미 확정)라 "저장"이 불필요함을 확인. 진짜 랜덤은 말풍선 폭/위치뿐인데, 이를 "서버가 1회 계산해 이벤트에 저장"(원래 설계, 프로토콜 확장 필요)할지 "클라이언트가 내용 기반 시드로 항상 같은 값을 재계산"(현재 방식 유지, 변경 없음)할지 사용자에게 선택지를 제시해 후자로 확정. `apps/web/src/panelBalloonLayout.ts`에 `computePanelBalloonLayout(panel)`을 순수 함수로 분리(React/Konva 비의존)하고, 같은 내용의 패널을 매번 새 객체로 구성해도 결과가 항상 동일함을 테스트로 증명 — 이 작업으로 `apps/web`에 vitest를 처음 도입(다른 워크스페이스와 동일 버전, 4개 테스트). 브라우저에서 완전히 독립된 두 세션(다른 유저, 다른 페이지 로드)이 같은 패널을 픽셀 단위로 동일하게 렌더링함을 줌인 비교로 확인. `docs/phases/03-full-auto-layout.md`의 완료 조건 갱신 — talkTo 기반 캐릭터 회전은 프로토콜에 addressing 필드가 아직 없어 미달성으로 명시하고 Phase 4로 이연. 워크스페이스 전체 typecheck/test(173개)/lint 클린.
- **2026-07-19** — 사용자 피드백("말풍선 크기가 채팅 내용과 상관없이 동일해 보임")으로 Stage 3 렌더링 버그 발견·수정. 원인은 `balloon/layout.ts`의 기본 지오메트리 상수(원작 ~2300-unit 스케일용 `widthFudge=200` 등)를 훨씬 작은 `PanelCanvas`(maxWidth≈284) 스케일에 그대로 넘겨써서, `widthFudge`가 실제 폭의 70%를 차지해 대부분의 메시지가 거의 같은 폭으로 클램프됐던 것. `PanelCanvas`가 이 패널 스케일에 맞게 비례 축소한 `geometry` 오버라이드를 `layoutPanelBalloons`에 넘기도록 수정 — 세로 예산을 성급하게 같이 줄였다가 5개 말풍선 패널에서 뒤쪽이 잘리는 회귀를 만들었지만, 짧은 텍스트 분기가 그 예산과 무관하고 긴 텍스트 분기도 `estimateWidestWordWidth`로 최소폭이 보장됨을 재확인해 예산은 넉넉하게 되돌렸다. 브라우저에서 길이가 다른 메시지들로 재확인해 폭이 뚜렷하게 달라지고 5개 말풍선 모두 안 잘리고 표시됨을 확인, 콘솔 에러 없음.
- **2026-07-19** — Phase 3 3단계(말풍선 랜덤화 + Konva 커스텀 모양) 완료. `packages/comic-engine/src/balloon/layout.ts`에 `estimateBalloonSize()`(`GetCloudEstimate` 포팅: 짧은 텍스트는 확정폭, 긴 텍스트는 면적 역산 minWidth~maxWidth 랜덤폭, arrowX를 항상 덮는 X배치)와 `layoutPanelBalloons()`(여러 말풍선을 겹치지 않게 순서대로 아래로 쌓는 단순화된 충돌 회피, 안 들어가면 `leftOver` 반환) 작성. `text/textMetrics.ts`에 `estimateTextWidth`/`estimateWidestWordWidth`/`splitTextToFit`(`CLabel::SplitHeight` 포팅) 추가. `apps/web`의 `SpeechBubble`을 `Konva.Shape`의 `sceneFunc` 기반 오돌토돌한 구름 테두리+꼬리로 교체, `PanelCanvas`를 `HistoryEntry` 1:1 렌더링에서 실제 `Panel`(다중 body+다중 balloon) 렌더링으로 재구성 — `ChatRoom`이 클라이언트에서 직접 `foldEvents()`를 돌려 패널을 재구성(서버와 동일 로직 재사용, 프로토콜 변경 없음). Stage 3 착수 전 원본 재대조 및 실통합 과정에서 진짜 버그 2건 발견·수정: (1) `zoom.ts`의 `BodyBox.top`/`.bottom`이 원작과 반대로 뒤바뀌어 있던 것(렌더러가 아직 없어 드러나지 않았던 잠재 버그), (2) `fold.ts`가 그리디 배치 순서를 `panel.bodies` 배열에 반영하지 않던 것(flip만 패치, 삽입 순서 그대로 — 4명이 순서대로 합류하는 실제 시나리오로 재현해 회귀 테스트 고정). 브라우저 E2E로 실제 발견한 문제 하나 더: 5개 말풍선이 쌓인 실제 패널에서 고정 캔버스 높이 때문에 뒤의 3개 말풍선이 조용히 잘려 안 보이는 버그를 발견 → 캔버스 높이를 배치 결과에 맞춰 동적으로 늘리도록 수정, 재확인 결과 5개 전부 정상 표시. 이 세 버그 모두 "실제로 켜서 눈으로 보기 전엔 안 드러났을" 종류라, 코드 리뷰만으로는 못 잡았을 것들. 워크스페이스 전체 typecheck/test(169개)/lint 클린.
- **2026-07-19** — 다국어(특히 한글) 채팅 지원 여부를 사용자 요청으로 재점검. 두 가지 실사용 이슈 발견: (1) `SpeechBubble.tsx`의 줄 수 추정이 라틴 문자 전용 고정 비율(fontSize*0.55)이라 한글/CJK처럼 정사각형에 가깝게 렌더링되는 문자에서 줄 수를 과소평가해 텍스트가 말풍선 밖으로 넘칠 수 있음(실제 렌더링 버그) — 사용자 승인으로 즉시 수정. (2) 감정 인식 규칙엔진이 원작 STRINGTABLE을 그대로 포팅한 영문 전용 설계(CheckWord/CheckStart/AllCaps)라 한글 문장은 대부분 NEUTRAL로 떨어짐 — 이건 설계 확장이 필요한 사안이라 사용자 판단으로 이번엔 보류, 추후 별도 논의하기로 함. `packages/comic-engine/src/text/textMetrics.ts`에 `estimateWrappedLineCount()` 추가(문자별로 넓은 문자(한글 음절/자모, CJK 통합 한자, 가나 등 East Asian Wide 대역)인지 판별해 폭을 다르게 근사, 공백 기준 word-wrap 시뮬레이션 + 공백 없는 긴 덩어리 내부 강제 줄바꿈까지 처리) 및 테스트 7개 작성, `SpeechBubble.tsx`가 이를 사용하도록 교체. 브라우저 E2E로 긴 한글 문장("안녕하세요 오늘 날씨가 정말 좋네요 우리 같이 산책하러 갈까요")이 말풍선 안에 정확히 담기는 것을 스크린샷으로 확인, 콘솔 에러 없음. 워크스페이스 전체 typecheck/test(145개)/lint 클린.
- **2026-07-19** — Phase 3 2단계에서 의도적으로 뺐던 `room_snapshot` 캐시 테이블을 사용자 요청으로 추가 구현(멀티룸/Phase 4 대비). `apps/server/src/eventStore.ts`에 `room_snapshot(room_id, seq, state_json, updated_at)` 테이블과 `saveSnapshot`/`loadSnapshot`/`loadSince`(seq 이후 이벤트만 조회, `append`도 부여된 seq를 반환하도록 변경) 추가. `Room`이 매 `say()`마다 그 시점 fold 결과를 스냅샷 저장하고, 생성자는 스냅샷이 있으면 그 이후분만 재fold하도록 변경 — 오래 지속된 방을 다시 열 때 이벤트 전체를 처음부터 재fold하지 않아도 된다. 스냅샷이 실제로 fold의 시작점으로 쓰이는지(존재만 하고 무시되지 않는지) 검증하려고, 일부러 틀린 스냅샷을 주입해 결과가 그 스냅샷을 기준으로 계산됨을 증명하는 테스트를 추가(단순히 "복구 결과가 맞다"만으로는 fold의 결합법칙 때문에 스냅샷을 무시하고 매번 전체 재fold해도 같은 결과가 나와 구분이 안 됨 — 이 점을 인지하고 화이트박스 테스트로 보완). "패널 폭 변경 시 무효화"는 아직 구현하지 않음을 명시(현재 스냅샷 내용이 폭/줌에 의존하지 않아 필요 없음, 3단계에서 그 의존성이 생기면 재검토). 워크스페이스 전체 typecheck/test(138개)/lint 클린.
- **2026-07-19** — Phase 3 2단계(이벤트 소싱 백엔드) 완료. `apps/server/src/eventStore.ts`에 SQLite(`better-sqlite3`, WAL 모드) `events(room_id, seq, ts, actor_id, type, payload_json)` append-only 로그 작성(`payload_json`에 `HistoryEntry` 원문을 그대로 저장해 replay 시 재계산 불필요). `packages/comic-engine/src/panel/fold.ts`에 `foldEvents()` 순수 함수 작성 — 1단계의 `shouldStartNewPanel`/`fetchSpeaker`/`doGreedyOrdering`/`updateHysteresis`를 그대로 재사용해 이벤트 로그를 패널 목록으로 재구성(증분 fold 지원: `foldEvents([신규이벤트], 이전결과)`가 전체 재fold와 동일 결과를 냄을 테스트로 증명). `Room`이 생성자에서 `EventStore.loadAll()`을 한 번 fold해 복구하고 이후 매 `say()`마다 증분 fold하도록 재구성. 새 프로토콜 메시지 `history`(서버→클라)를 추가해 `join` 직후 이전 대화 전체를 그 클라이언트에게만 재생 — `apps/web`의 `useRoomConnection`도 대응 수정. 브라우저 E2E로 실제 재접속 replay를 확인(Alice가 2줄 발화 → 새로고침 → 별도 세션 Bob으로 재입장 → 동일한 2패널이 그대로 재구성, 콘솔 에러 없음, SQLite 파일에 실제 영속화 확인). `room_snapshot` 캐시 테이블은 의도적으로 만들지 않기로 결정(단일 room·낮은 트래픽 규모에서 메모리 내 증분 fold만으로 충분하고 무효화 로직도 필요 없어짐 — CLAUDE.md의 "필요한 것만 만든다" 원칙, Phase 4 멀티룸 확장 시 재검토 예정)하고 이유를 `docs/phases/03-full-auto-layout.md`에 기록. `better-sqlite3` 네이티브 빌드 스크립트를 `pnpm-workspace.yaml`의 `allowBuilds`에 승인 추가. 워크스페이스 전체 typecheck/test(132개)/lint 클린.
- **2026-07-19** — Phase 3 1단계(패널 클론/그리디 배치/줌) 완료. `packages/comic-engine/src/panel/`에 `types.ts`(actorId 문자열 참조로 원작의 speaker 포인터 재연결 문제 자체를 제거), `panel.ts`(`shouldStartNewPanel`/`fetchSpeaker`), `placement.ts`(`addTalkTos`/`evalPair`/`evalPlacement`/`doGreedyOrdering`/`updateHysteresis` — `EvalPair` 페널티 상수를 원작과 완전히 동일하게 포팅, 죽은 필드 `m_priority`는 미포팅), `zoom.ts`(`isEstablishing`/`layoutBodies` — `LayoutAvatars`의 신장정규화→축소/확대→1.1배 스냅→마진배치를 분리 포팅, 렌더링 전용인 `AdjustArtToCoord`는 범위 밖으로 판단해 제외)를 작성. 테스트 작성 중 두 차례 자체 오류 발견·수정: (1) 3인 히스테리시스 시나리오를 손으로 계산하다 예측이 틀렸음을 깨닫고 추측성 테스트 대신 결정성(동일 입력→동일 출력) 테스트로 교체, (2) `evalPair` 테스트 하나가 기댓값을 잘못 손계산(40 vs 실제 44) — 원작 `EvalPair` 소스로 재대조해 코드가 아니라 테스트 쪽 계산이 틀렸음을 확인 후 정정. `comic-engine` 84개 테스트 전부 통과. 이 작업으로 워크스페이스 전체 typecheck를 처음 돌려보다 `asset-manifest-types`가 `structuredClone` 전역 타입을 못 찾는 기존 문제(우연히 vitest의 peer로만 `@types/node`가 들어와 있던 상태)를 발견, `comic-engine`/`asset-manifest-types` 양쪽에 `@types/node`를 명시적으로 추가해 해결 — 워크스페이스 전체 typecheck(6개 프로젝트)와 test 모두 클린. `docs/phases/03-full-auto-layout.md`의 1단계 체크리스트 갱신.
- **2026-07-18** — Phase 3 착수, `panel.cpp`(1434줄)/`balloon.cpp` 스파이크 완료. 핵심 발견: (1) 패널 클론 조건이 plan.md 초안과 반대(같은 화자 연속=새 패널, 새 화자 합류=클론). (2) 첫 1~2패널은 "설정샷"이라 줌인 비활성. (3) 그리디 배치(`EvalPair`/`EvalPlacement`) 정확한 페널티 상수 확보. (4) 말풍선 크기 추정에서 죽은 코드(`canBeTall` 분기) 발견 — 실제 동작은 minWidth~maxWidth 랜덤 폭. (5) route-region 충돌 회피는 계획대로 단순화해 재구현하기로 확정. 사용자와 협의해 4단계 진행 순서 확정: 1)패널클론/그리디배치/줌 → 2)이벤트소싱(SQLite) → 3)말풍선랜덤화+Konva → 4)시드재현성+E2E. `plan.md`/`docs/phases/03-full-auto-layout.md`에 반영 완료.

- **2026-07-18** — Phase 2 전체 완료. `packages/comic-engine/src/avatar/bodyBox.ts`에 `GetBodyBox`(얼굴/몸통 상대위치+스케일+하단정렬) 포팅. `apps/web`에 `react-konva` 기반 `PanelCanvas`/`AvatarSprite`/`SpeechBubble` 구현, `NicknameGate`에 22개 캐릭터 아이콘 선택 UI 추가, `catalog.json` fetch 훅(`useAssetCatalog`) 작성. `apps/server`의 `Room`이 `matchComplexPose`/`matchSimplePose`로 액터별 포즈를 계산해 `historyEntry`에 포함하도록 확장(액터별 라운드로빈 상태 보관), `join` 프로토콜에 `characterId` 추가. 브라우저 2탭 E2E로 Complex(Mike, SHOUT/LAUGH 표정 전환)와 Simple(Tux, 전신 스프라이트) 렌더링, 배경(room8bs) 합성, 말풍선까지 전부 스크린샷으로 확인, 콘솔 에러 없음. 워크스페이스 전체 테스트 89개 + typecheck + lint 클린. `docs/phases/02`의 완료조건 중 "동일 감정 반복 시 라운드로빈" 항목은 원작 재확인 결과 잘못된 가정이었음을 밝히고 정정(라운드로빈은 NEUTRAL 폴백에서만 작동).
- **2026-07-18** — Phase 2 "포즈 매칭 엔진" 완료. `packages/comic-engine/src/avatar/`에 `emotionWheel.ts`(각도 유틸)와 `matcher.ts`(`matchComplexPose`/`matchSimplePose`) 작성. `avatar.cpp`를 재추적하다 중요한 설계 오류를 발견·수정: 원작의 실제 매칭 함수는 감정 후보 하나가 아니라 `resolveEmotion()`의 전체 후보 리스트를 우선순위 순으로 소비하며 얼굴/몸통을 각각 다른 후보로 채울 수 있음(SHOUT→얼굴, POINTOTHER→몸통처럼). 또한 라운드로빈은 매칭이 아니라 NEUTRAL 폴백에만 쓰인다는 것도 확인. `docs/phases/02-static-avatar-rendering.md`에 정정 근거 기록. 단위 테스트 16개 + 실제 Mike 매니페스트로 교차검증 완료.
- **2026-07-18** — Phase 2 `.avb` 컨버터(`tools/avb-converter`) 구현 및 실전 변환 완료. `bmpDecoder.ts`(1bpp/4bpp-RLE4), `avbParser.ts`(태그 스트림+ditto+감정 인덱스 매핑), `compositor.ts`(MERGEPAINT+SRCAND→알파합성, HEADMASK/TORSOMASK 게이팅), `manifest.ts` 작성. 새 공용 타입 패키지 `packages/asset-manifest-types` 추가. 22개 아바타 + 배경 3종 전량을 실제로 변환해 `apps/web/public/assets/`에 커밋(PNG 367개, 2.3MB). 구현 중 발견/수정한 실측 이슈 2건: (1) mask/aura가 fgnd와 원본 해상도가 다를 수 있음(원작이 `StretchDIBits`로 동일 목적지 사각형에 맞춰 그리기 때문) → 최근접 이웃 리샘플링으로 해결. (2) `avatario.cpp`의 `LoadAvatarInfo`가 내부 `AK_NAME` 태그를 버리고 `.avb` 파일명을 진짜 이름으로 쓴다는 사실을 재검증 중 발견(`glenda.avb`의 내부 이름은 "Greg") → 컨버터가 파일명을 채택하도록 수정. 매 단계 실제 원작 파일로 구현체를 직접 교차검증(헥스 수동 분석·22개 파일 전수 파싱·색상 배경에 합성해 육안 확인). 테스트 17개 통과.
- **2026-07-18** — Phase 2 착수, `.avb`/배경 BMP 포맷 스파이크 완료. `avatario.cpp`/`dib.cpp`/`bodycam.cpp`를 직접 추적하고 22개 아바타 `.avb` 전수 + 배경 3종 전수를 실제로 파싱·렌더링해 검증. 핵심 발견: (1) 캐릭터 아트는 전부 1bpp `BI_RGB` 비압축(RLE 불필요), 배경·아이콘만 4bpp `BI_RLE4`, `BI_RLE8`은 v1.0-pre에서 전혀 안 씀 — RLE8 디코더 구현 생략 결정. (2) 아바타 아트는 흑백 잉크선 라인아트이며 `MERGEPAINT`(mask/aura, 아바타별 `HEADMASK`/`TORSOMASK` 플래그로 게이팅)+`SRCAND`(fgnd) GDI 합성을 알파합성으로 치환하는 정확한 알고리즘 확정. (3) 얼굴+몸통 상대위치 계산식(`GetBodyBox`) 확보 — 자산에 굽지 않고 렌더러에 포팅하기로 결정. `plan.md`/`docs/phases/02-static-avatar-rendering.md`에 반영 완료.
- **2026-07-18** — 원작 소스 재검증 중 `ChatPreSendText`(`UI::PreSay`)의 "타이핑 중 실시간 반응" UX가 Phase 1 웹 클라이언트에 없음을 확인, 사용자 요청으로 즉시 추가. `apps/web/src/useLocalEmotionPreview.ts`가 `comic-engine`의 `resolveEmotion()`을 서버 왕복 없이 로컬에서 직접 호출해, 전송 전 입력창 옆에 감정 미리보기(`미리보기: LAUGH(11)` 등)를 표시. `claude-in-chrome`으로 전송 버튼을 누르기 전 미리보기가 뜨는 것과 전송 후 정리되는 것을 스크린샷으로 확인.
- **2026-07-18** — Phase 1 전체 완료. `apps/web`(Vite+React+TS)에 `useRoomConnection` 훅, 닉네임 입장 화면(`NicknameGate`), 메시지 리스트+감정 라벨 배지(`ChatRoom`) 구현. 서버+클라이언트를 실제로 띄워 `claude-in-chrome`으로 두 브라우저 탭 E2E 테스트 수행: 서로 다른 닉네임 입장 → memberList 실시간 동기화 → `ROTFL that's great :)`(LAUGH 11)/`THIS IS AMAZING`(SHOUT 9)/무매칭 문장(라벨 없음) 모두 두 탭에 동일하게 실시간 반영됨을 스크린샷으로 확인, 콘솔 에러 없음. 워크스페이스 전체 테스트 48개 + typecheck + lint 클린. Phase 1 완료 조건(두 탭 대화, STRINGTABLE 규칙 테스트 통과, 무지연 감정 라벨) 전부 충족.
- **2026-07-18** — Phase 1 "WebSocket 서버" 완료. `apps/server`(Node+ws)에 단일 room(`Room` 클래스: join/leave/say, 인메모리 이벤트 로그) 구현. `say()`가 `comic-engine`의 `resolveEmotion()`을 호출해 `historyEntry`로 브로드캐스트. 유닛 테스트 6개 + 실제 WS 연결로 두 클라이언트(join→memberList, say→historyEntry 동일 수신) 스모크 테스트 통과.
- **2026-07-18** — Phase 1 "프로토콜 스키마" 완료. `packages/protocol/src/schema.ts`에 zod로 클라→서버 `join`/`say` 액션과 서버→클라 `historyEntry`/`memberList` 브로드캐스트 정의. `emotion` 필드는 `comic-engine`의 `EmotionCandidate`(emotion/intensity/priority)와 `ALL_EMOTION_IDS`를 그대로 재사용해 타입 어긋남 방지. 테스트 8개 통과.
- **2026-07-18** — Phase 1 "감정 규칙 엔진 포팅" 완료. `packages/comic-engine/src/rules/`에 `matchers.ts`(checkWord/findString/checkStart/checkAllCaps/getSentenceStarts), `ruleEngine.ts`(loadRules/resolveEmotion), `rules.default.json`(v1.0-pre `chat.rc` STRINGTABLE ID_RULE_* 원문 이식) 작성. `textpose.cpp`/`avatar.h`와 대조 검증 완료(우선순위 override 처리 순서 버그 1건 테스트 작성 중 발견·수정). 단위 테스트 34개 통과.
- **2026-07-18** — Phase 1 "리포지토리 셋업" 완료. pnpm workspace, 루트 tsconfig/eslint/prettier, `packages/comic-engine`·`packages/protocol` 스캐폴딩.

- **2026-07-18** — 계획 수립 완료. 원작 소스(`comic-chat`) 분석 기반으로 `plan.md` 및 6단계 작업계획(`docs/phases/`) 작성. 구현 착수 전 상태.

## 다음 액션

- [ ] Phase 4("전체 프로토콜 + 멀티룸") 착수(`docs/phases/04-full-protocol-multiroom.md`) — 착수 전 문서의 작업 목록·완료 조건 확인
- [ ] Phase 4 범위에 포함해 검토: (1) talkTo/addressing 프로토콜 필드 추가 후 `fold.ts`에 배선(Phase 3에서 의도적으로 미룸), (2) `zoom.ts`의 정밀 아바타 배치(신장 정규화/줌 스냅/`GetDimInfo`)를 실제 렌더러(`PanelCanvas`)에 연결 — 지금은 동일 폭 칸 배치로 단순화됨, (3) 멀티룸 도입 시 `room_snapshot` 캐시(Phase 3에서 미리 준비해둠)가 실제로 room 지연 생성/정리와 맞물려 잘 동작하는지 확인
